{
  "id": "edc02e21",
  "title": "Replace reactive starfield with HDR_multi_nebulae_1.hdr background in CarouselScene.ts",
  "tags": [],
  "status": "done",
  "created_at": "2026-02-15T22:03:00.699Z"
}

## Summary
Remove the `ReactiveStarfield` usage from `CarouselScene.ts` and replace it with an HDR environment background.

## Steps
1. **Remove ReactiveStarfield usage** from `src/components/simulation/CarouselScene.ts`:
   - Remove the import of `ReactiveStarfield`
   - Remove the `reactiveStarfield` property
   - Remove construction in the constructor (lines ~93-98)
   - Remove `setResolution` call on resize (line ~245)
   - Remove `dispose()` call in cleanup (line ~326)

2. **Load `HDR_multi_nebulae_1.hdr`** as equirectangular environment map:
   - Use `RGBELoader` to load `/textures/HDR_multi_nebulae_1.hdr`
   - Set `texture.mapping = THREE.EquirectangularReflectionMapping`
   - Assign to both `scene.background` and `scene.environment`

## Notes
- Do **not** delete the `ReactiveStarfield` class file or its shader/util files — only remove usage from `CarouselScene.ts`
- The HDR file already exists at `public/textures/HDR_multi_nebulae_1.hdr`
